<%= form_with(model: timelog, local: true) do |form| %>
  <% if timelog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timelog.errors.count, "error") %> prohibited this timelog from being saved:</h2>

      <ul>
        <% timelog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project %>
    <%= collection_select(:timelog, :project_id, Project.all, :id, :name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :user %>
    <%= collection_select(:timelog, :user_id, User.all, :id, :email, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
